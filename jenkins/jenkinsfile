pipeline {
    agent any

    stages {
        stage('Unit Test - Backend') {
            steps {
                echo 'Hello World'
                sh 'echo "Hello Jenkins"'
                sh 'date'
            }
        }
    }
}
